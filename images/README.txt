== ImageToC Usage ==

This tool converts images into C/C++ files located in the src/gui/lib/ directory.
Add a .png file into this directory and run the tool to convert into icon/image.
A .png file prefixed with "cursor_" gets converted into a cursor.

The tool assumes that cwd is this directory, as well as the src/gui/lib/ directory exists.